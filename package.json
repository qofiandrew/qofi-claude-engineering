{
  "name": "qofi-claude-engineering",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "_comment_test": "The repository's canonical check runs Bun tests and every shell integration test through .claude/test-cmd.",
    "test": "bash .claude/test-cmd",
    "test:unit": "bun test",
    "audit:production": "cd bridge && bun audit --production && cd ../codex-bridge && bun audit --production"
  }
}

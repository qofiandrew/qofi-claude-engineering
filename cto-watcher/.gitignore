# Secrets and operator-specific config — never commit
.env
config.json

# Dead-letter files — dropped relay bodies can carry sensitive content (ADR-0010);
# chmod 600, recoverable by the operator, never committed.
.deadletter/
checkin-metrics.jsonl

# Node
node_modules
npm-debug.log*

# pm2 local logs (if any land here)
*.log

# cto-watcher/.env — the WATCHER's own Discord bot token.
# This is a SEPARATE bot identity from the CPO bot and from every CTO swarm bot.
# Copy this file to .env and paste the token. NEVER commit .env (it is gitignored).
#
#   cp .env.example .env
#
DISCORD_BOT_TOKEN=

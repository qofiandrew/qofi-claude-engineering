# TEST_MAP — `<module>`

Where this module's behavior is tested. Every path must exist (the
`canon-check` gate verifies backtick-quoted `tests/…` / `src/…` paths).

| Test path | What it covers |
|-----------|----------------|
| `tests/<path>` | `<one line>` |

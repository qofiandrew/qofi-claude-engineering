# LIFECYCLE — `<module>`

States and transitions this module's objects move through, with canon
citations. Only present where the module has a stateful lifecycle — delete
this file for stateless modules (it is the one optional pack member).

| State | Enters via | Exits via | Canon ref |
|-------|-----------|-----------|-----------|

# CODE_MAP — `<module>`

Where this module lives in the tree. Every path must exist (the `canon-check`
gate verifies backtick-quoted `src/…` / `tests/…` paths).

| Path | Role |
|------|------|
| `src/<module>/<file>` | `<one line>` |

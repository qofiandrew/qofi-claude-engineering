# TRACEABILITY_LEDGER — canon → modules → tests

Cross-module traceability: which canon requirements/ADRs are implemented
where, and which tests prove them. Per-module detail lives in each pack's
`CANON_MAP.md` / `TEST_MAP.md`; this ledger is the cross-cutting roll-up.

| Canon ref (ADR / requirement) | Module(s) | Tests | Status |
|-------------------------------|-----------|-------|--------|
| `<canon-path or ADR-id>` | `<module>` | `<test path or GAP-id>` | implemented / partial / gap |

# <Product> — Security

> DEFINITION: this product's security REQUIREMENTS — the readiness bar (authz/authn,
>   secrets handling, data protection, bounded blast radius) plus product-specific
>   security demands driven by the data it touches. The should-be; the CTO reports
>   the as-built.
> ROUTES HERE: data sensitivity, access rules, compliance/handling demands,
>   blast-radius expectations.
> GREP FOR: "what are product-N's security requirements?", "what data class /
>   access model applies?"
> WRITE CLASS: gated (security posture is core/high-risk). (Baseline: READINESS_BAR.md.)

# <Product> — Scale

> DEFINITION: the LOAD the product must hold — burst patterns, throughput, latency
>   expectations, volume shapes. The non-functional demand that is the CPO's
>   signature pushback ammunition (e.g. "1M files uploaded in a burst").
> ROUTES HERE: any statement about how much, how fast, how concurrent, how spiky —
>   the real-world load the product must survive.
> GREP FOR: "what load must product-N handle?", "what's the burst/throughput/
>   latency requirement?"
> WRITE CLASS: auto; gated if it sets a core scale target.
